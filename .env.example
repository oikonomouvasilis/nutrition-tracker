# ---- Supabase ----
# Project Settings → API
# Το publishable key είναι ασφαλές να εκτεθεί στον client (προστατεύεται από RLS).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# ---- Gemini (AI macro lookup — server-side μόνο) ----
# Δωρεάν key από https://aistudio.google.com/apikey (χωρίς κάρτα).
# ΜΗΝ το βάλεις με NEXT_PUBLIC_ πρόθεμα. Μένει server-side.
GEMINI_API_KEY=
