# ---- Supabase ----
# Project Settings → API
# Το anon key είναι ασφαλές να εκτεθεί στον client (προστατεύεται από RLS).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ---- Anthropic (AI macro lookup — server-side μόνο, μελλοντική φάση) ----
# ΜΗΝ το βάλεις με NEXT_PUBLIC_ πρόθεμα. Μένει server-side.
# ANTHROPIC_API_KEY=
